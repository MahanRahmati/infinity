/// Represents different interaction states for UI elements.
enum InteractionState {
  /// The element is in a hovered state.
  hover,

  /// The element is in a focused state.
  focused,

  /// The element is in a pressed state.
  pressed,

  /// The element is in a disabled state.
  disabled,
}
